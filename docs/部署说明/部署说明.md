# TradingAgents 项目部署说明

## 目录

1. [概述](#1-概述)
2. [服务器信息](#2-服务器信息)
3. [部署方式](#3-部署方式)
4. [首次部署](#4-首次部署)
5. [日常更新](#5-日常更新)
6. [服务管理](#6-服务管理)
7. [环境变量](#7-环境变量)
8. [Web 服务接口](#8-web-服务接口)
9. [OpenClaw Skill](#9-openclaw-skill)
10. [One-API 网关（可选）](#10-one-api-网关可选)
11. [常用命令速查](#11-常用命令速查)
12. [故障排查](#12-故障排查)

---

## 1. 概述

TradingAgents 部署在 Google Cloud 服务器上，运行 Web API 服务（FastAPI + Uvicorn），对外提供股票分析 API 和 OpenClaw Skill 托管。

**部署通过 Claude Code 在本地完成**，Claude Code 通过 SSH 远程执行服务器上的操作。

---

## 2. 服务器信息

| 项目 | 值 |
|------|-----|
| **服务器 IP** | `34.92.13.95` |
| **用户名** | `support` |
| **项目目录** | `/home/support/TradingAgents` |
| **虚拟环境** | `/home/support/TradingAgents/venv` |
| **Python 版本** | 3.10+ |
| **Web 服务端口** | `8000` |
| **Git 仓库** | `https://github.com/hangeaiagent/TradingAgents` |
| **SSH 密钥** | `$HOME/.ssh/id_rsa_google_longterm` |

---

## 3. 部署方式

### 通过 Claude Code 远程操作

所有部署操作均在本地通过 Claude Code 完成，Claude Code 使用 SSH 连接服务器执行命令。

**SSH 连接需指定密钥文件**：

```bash
# SSH 连接格式
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "命令"

# 需要多条命令时
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "cd /home/support/TradingAgents && 命令1 && 命令2"

# SCP 上传文件格式
scp -i $HOME/.ssh/id_rsa_google_longterm 本地文件 support@34.92.13.95:远程路径

# rsync 同步格式
rsync -avz -e "ssh -i $HOME/.ssh/id_rsa_google_longterm" 本地目录/ support@34.92.13.95:远程目录/
```

> 以下文档中所有 SSH/SCP/rsync 命令均使用 `-i $HOME/.ssh/id_rsa_google_longterm` 密钥。

**注意**：不需要手动 SSH 登录服务器操作，所有操作由 Claude Code 代为执行。

---

## 4. 首次部署

以下步骤由 Claude Code 通过 SSH 远程执行：

### 4.1 服务器环境准备

```bash
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "sudo apt update && sudo apt install -y python3.10 python3.10-venv python3-pip git"
```

### 4.2 克隆项目

```bash
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "cd /home/support && git clone https://github.com/hangeaiagent/TradingAgents.git"
```

### 4.3 创建虚拟环境并安装依赖

```bash
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "cd /home/support/TradingAgents && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt"
```

### 4.4 配置环境变量

通过 SCP 上传本地 `.env` 文件，或远程创建：

```bash
scp -i $HOME/.ssh/id_rsa_google_longterm /Users/a1/work/TradingAgents/.env support@34.92.13.95:/home/support/TradingAgents/.env
```

### 4.5 配置 systemd 服务

```bash
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "sudo tee /etc/systemd/system/tradingagents.service > /dev/null << 'EOF'
[Unit]
Description=TradingAgents Web API
After=network.target

[Service]
Type=simple
User=support
WorkingDirectory=/home/support/TradingAgents
Environment=PATH=/home/support/TradingAgents/venv/bin:/usr/bin:/bin
EnvironmentFile=/home/support/TradingAgents/.env
ExecStart=/home/support/TradingAgents/venv/bin/uvicorn web.app:app --host 0.0.0.0 --port 8000
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF"
```

### 4.6 启动服务

```bash
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "sudo systemctl daemon-reload && sudo systemctl enable tradingagents && sudo systemctl start tradingagents"
```

---

## 5. 日常更新

### 5.1 代码更新（Git 拉取）

最常用的更新方式，由 Claude Code 执行。**必须清除 `__pycache__` 缓存**，否则旧字节码可能导致新代码不生效：

```bash
# 标准部署流程（一条命令，不可省略清缓存步骤）
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "\
  cd /home/support/TradingAgents && \
  git pull origin main && \
  find . -name '__pycache__' -type d -exec rm -rf {} + 2>/dev/null; \
  sudo systemctl restart tradingagents && \
  sleep 2 && \
  sudo systemctl status tradingagents --no-pager"
```

如有依赖变更，在 `git pull` 之后、重启之前加：

```bash
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "cd /home/support/TradingAgents && source venv/bin/activate && pip install -r requirements.txt"
```

> **注意**：`find . -name '__pycache__' -exec rm -rf {} +` 是必须步骤。详见[服务器运维操作 - `__pycache__` 缓存问题](服务器运维操作.md#__pycache__-缓存导致代码未生效重要)。

### 5.2 上传特定文件（SCP）

当需要上传本地文件到服务器时：

```bash
# 上传单个文件
scp -i $HOME/.ssh/id_rsa_google_longterm /Users/a1/work/TradingAgents/文件路径 support@34.92.13.95:/home/support/TradingAgents/文件路径

# 上传目录
scp -i $HOME/.ssh/id_rsa_google_longterm -r /Users/a1/work/TradingAgents/目录 support@34.92.13.95:/home/support/TradingAgents/
```

### 5.3 批量同步（rsync）

同步整个项目（排除不需要的文件）：

```bash
rsync -avz -e "ssh -i $HOME/.ssh/id_rsa_google_longterm" \
  --exclude 'venv' --exclude '.git' --exclude '__pycache__' --exclude '.env' \
  /Users/a1/work/TradingAgents/ support@34.92.13.95:/home/support/TradingAgents/
```

---

## 6. 服务管理

所有命令通过 Claude Code 的 SSH 远程执行：

### 6.1 systemd 服务管理

```bash
# 启动
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "sudo systemctl start tradingagents"

# 停止
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "sudo systemctl stop tradingagents"

# 重启
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "sudo systemctl restart tradingagents"

# 查看状态
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "sudo systemctl status tradingagents"

# 查看日志（最近 50 行）
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "sudo journalctl -u tradingagents -n 50 --no-pager"

# 实时日志
ssh -t -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "sudo journalctl -u tradingagents -f"
```

### 6.2 手动启动（调试用）

```bash
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "cd /home/support/TradingAgents && source venv/bin/activate && uvicorn web.app:app --host 0.0.0.0 --port 8000"
```

### 6.3 后台运行（无 systemd 时）

```bash
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "cd /home/support/TradingAgents && source venv/bin/activate && nohup uvicorn web.app:app --host 0.0.0.0 --port 8000 > app.log 2>&1 &"
```

---

## 7. 环境变量

服务器上的 `/home/support/TradingAgents/.env` 文件需包含：

| 变量 | 必需 | 说明 |
|------|------|------|
| `OPENAI_API_KEY` | 至少配一个 LLM Key | OpenAI API Key |
| `GOOGLE_API_KEY` | 可选 | Gemini API Key |
| `ANTHROPIC_API_KEY` | 可选 | Claude API Key |
| `XAI_API_KEY` | 可选 | Grok API Key |
| `ONE_API_KEY` | 可选 | One-API 统一网关 Key（自动 fallback 为 OPENAI_API_KEY） |
| `ONE_API_BASE_URL` | 可选 | One-API 地址，如 `http://104.197.139.51:3000/v1` |
| `ONE_API_GEMINI_MODEL` | 可选 | One-API 代理的 Gemini 模型名 |

---

## 8. Web 服务接口

服务启动后可通过 `http://34.92.13.95:8000` 访问：

| 端点 | 方法 | 用途 |
|------|------|------|
| `/` | GET | 前端页面 |
| `/api/docs` | GET | OpenAPI 文档 |
| `/api/analyze/sync` | POST | 同步股票分析 |
| `/api/analyze` | POST | SSE 流式分析 |
| `/skills/{skill_name}.md` | GET | OpenClaw Skill 文件 |

---

## 9. OpenClaw Skill

### 9.1 部署模型

OpenClaw 由用户本地运行，我们只需托管 Skill 文件。Skill 文件通过 Web 服务的 `/skills/` 端点提供。

### 9.2 用户安装方式

```
# 在飞书对 OpenClaw 说
install skill from http://34.92.13.95:8000/skills/tradingagents-demo.md
```

### 9.3 Skill 版本

| Skill | API Key | 说明 |
|-------|---------|------|
| **Demo** | 不需要 | 截断到 2000 字符，供体验 |
| **Pro** | 需 `AGENTPIT_API_KEY` + `AGENTPIT_AGENT_ID` | 完整报告 |

---

## 10. One-API 网关（可选）

| 项目 | 地址 |
|------|------|
| One-API 管理界面 | `http://104.197.139.51:3000` |
| API 基础路径 | `http://104.197.139.51:3000/v1` |

One-API 对外暴露 OpenAI 兼容接口，用于：
- 解决 Gemini 直连 IP 限制
- 统一多模型 API 格式
- 集中管理 API Key 与用量

---

## 11. 常用命令速查

以下命令均由 Claude Code 执行：

| 操作 | 命令 |
|------|------|
| 检查服务状态 | `ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "sudo systemctl status tradingagents"` |
| 拉取最新代码 | `ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "cd /home/support/TradingAgents && git pull origin main"` |
| 重启服务 | `ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "sudo systemctl restart tradingagents"` |
| 查看日志 | `ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "sudo journalctl -u tradingagents -n 50 --no-pager"` |
| 上传 .env | `scp -i $HOME/.ssh/id_rsa_google_longterm .env support@34.92.13.95:/home/support/TradingAgents/` |
| 检查端口 | `ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "lsof -i :8000"` |
| 安装依赖 | `ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "cd /home/support/TradingAgents && source venv/bin/activate && pip install -r requirements.txt"` |

---

## 12. 故障排查

### 服务启动失败

```bash
# 查看详细错误
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "sudo journalctl -u tradingagents -n 100 --no-pager"

# 手动启动看报错
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "cd /home/support/TradingAgents && source venv/bin/activate && python -c 'from web.app import app; print(\"OK\")"
```

### 端口被占用

```bash
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "lsof -i :8000"
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "kill -9 <PID>"
```

### 依赖缺失

```bash
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "cd /home/support/TradingAgents && source venv/bin/activate && pip install -r requirements.txt"
```

### 验证服务是否正常

```bash
ssh -i $HOME/.ssh/id_rsa_google_longterm support@34.92.13.95 "curl -s http://localhost:8000/ | head -20"
```
